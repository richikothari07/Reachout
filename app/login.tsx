'use client'
import { FormEvent, useState } from 'react'
import { supabaseBrowser } from '@/lib/supabase-browser'
export default function Login({onAuthed}:{onAuthed:()=>void}){
 const [mode,setMode]=useState<'login'|'signup'|'reset'>('login'),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[busy,setBusy]=useState(false),[error,setError]=useState(''),[success,setSuccess]=useState('')
 const submit=async(e:FormEvent)=>{e.preventDefault();setBusy(true);setError('');setSuccess('');try{if(mode==='reset'){const {error}=await supabaseBrowser.auth.resetPasswordForEmail(email,{redirectTo:`${window.location.origin}/reset-password`});if(error)throw error;setSuccess("If an account exists for this email, we've sent a password reset link. Check your inbox.");return}const r=mode==='login'?await supabaseBrowser.auth.signInWithPassword({email,password}):await supabaseBrowser.auth.signUp({email,password});if(r.error)throw r.error;if(mode==='signup'&&!r.data.session){setSuccess('Account created. Check your email to confirm your account, then sign in.');return}onAuthed()}catch(e){setError(e instanceof Error?e.message:'Could not complete that request.')}finally{setBusy(false)}}
 return <main className="authPage"><div className="authCard"><img src="/reachout-logo.png" className="authLogo"/><span className="authEyebrow">OUTREACH INTELLIGENCE</span><h1>{mode==='login'?'Welcome back.':mode==='signup'?'Create your ReachOut account.':'Reset your password.'}</h1><p>{mode==='reset'?'Enter your email and we'll send you a secure password reset link.':'Keep your network, outreach history and follow-ups saved securely across devices.'}</p><form onSubmit={submit}><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required placeholder="you@example.com"/></label>{mode!=='reset'&&<label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required minLength={6} placeholder="••••••••"/></label>}{error&&<div className="authError">{error}</div>}{success&&<div className="authSuccess">{success}</div>}<button className="primary full" disabled={busy}>{busy?'Please wait…':mode==='login'?'Sign in':mode==='signup'?'Create account':'Send reset link'}</button></form>{mode==='login'&&<button className="forgotPassword" onClick={()=>{setMode('reset');setError('');setSuccess('')}}>Forgot password?</button>}<button className="authSwitch" onClick={()=>{setMode(mode==='signup'?'login':'signup');setError('');setSuccess('')}}>{mode==='signup'?'Already have an account? Sign in':mode==='reset'?'Back to sign in':'New to ReachOut? Create your account'}</button>{mode==='reset'&&<p className="authHint">You’ll receive a secure link if the email is registered.</p>}</div></main>
}
