import { NextResponse } from 'next/server'
import { authenticatedUser, supabaseAdmin, ensureImportBucket } from '@/lib/supabase-server'
import { importCsvText } from '@/lib/importer'
export const runtime='nodejs'; export const maxDuration=60
export async function POST(req:Request){try{const user=await authenticatedUser(req);const form=await req.formData();const file=form.get('file');if(!(file instanceof File))return NextResponse.json({error:'file is required.'},{status:400});if(!file.name.toLowerCase().endsWith('.csv'))return NextResponse.json({error:'Please upload a CSV file.'},{status:400});if(file.size>4*1024*1024)return NextResponse.json({error:'FILE_TOO_LARGE',useSignedUpload:true},{status:413});const s=supabaseAdmin();await ensureImportBucket(s);const result=await importCsvText(s,user.id,file.name,await file.text());return NextResponse.json({ok:true,...result})}catch(e){console.error('IMPORT_UPLOAD_ERROR',e);return NextResponse.json({error:e instanceof Error?e.message:'Could not import file.'},{status:401})}}
